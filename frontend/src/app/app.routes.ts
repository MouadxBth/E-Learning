import { Routes } from '@angular/router';
import { CourspageComponent } from './courspage/courspage.component';

export const routes: Routes = [
    {path:"course_page", component: CourspageComponent}
];
